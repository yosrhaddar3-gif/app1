version: "3"

services:
  app:
    build: .
    container_name: todo-app
    ports:
      - "3001:3000"
    restart: always

  mongodb:
    image: mongo
    ports:
      - "27017:27017"
    restart: always